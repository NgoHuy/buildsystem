# Maintainer: Severus <severus@theslinux.org>
# Original Contributor: Manolis Tzanidakis
# Original Contributor: Mark Rosenstand <mark@archlinux.org>
# Original Contributor: Thayer Williams <thayer@archlinux.org>
# Original Maintainer: Gaetan Bisson <bisson@archlinux.org>

pkgdesc='Lightweight C library that eases the writing of UNIX daemons'
url='http://0pointer.de/lennart/projects/libdaemon/'
license=('LGPL')
arch=('i686' 'x86_64')
source=("http://0pointer.de/lennart/projects/libdaemon/${pkgbase}-${pkgver}.tar.gz")
options=('!libtool')

build() {
	cd "${srcdir}/${pkgbase}-${pkgver}"
	./configure --prefix=/usr --localstatedir=/var --disable-lynx --disable-static
	make
}

package() {
	cd "${srcdir}/${pkgbase}-${pkgver}"
	make DESTDIR="${pkgdir}" install
}

