# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Ronald van Haren <ronald.archlinux.org>
# Original Contributor: Jan de Groot <jgc.archlinux.org>
# Original Contributor: Eric Johnson <eric.archlinux.org>

pkgdesc="Implementation of the Stringprep, Punycode and IDNA specifications"
url="http://www.gnu.org/software/libidn/"
arch=('i686' 'x86_64')
license=('GPL3' 'LGPL')
depends=('glibc' 'texinfo')
makedepends=('pkgconfig')
options=('!libtool')
source=(http://ftp.gnu.org/gnu/${pkgbase}/${pkgbase}-${pkgver}.tar.gz)
install=libidn.install
	
build() {
  cd ${srcdir}/${pkgbase}-${pkgver}
  ./configure --prefix=/usr
  make
}

package() {
  cd ${srcdir}/${pkgbase}-${pkgver}
  make DESTDIR=${pkgdir} install
}
