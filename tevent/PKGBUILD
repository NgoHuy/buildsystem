# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Tobias Powalowski <tpowa@archlinux.org>
# Original Contributor: Christian Hesse <mail@eworm.de>
# Original Contributor: Marco A Rojas <marquicus at gmail dot com>
# Original Contributor: Ng Oon-Ee <ngoonee.talk@gmail.com>
# Original Contributor: Thomas Burdick <thomas.burdick@gmail.com>

pkgdesc="An event system based on the talloc memory management library"
arch=('i686' 'x86_64')
url="https://tevent.samba.org/"
source=("http://samba.org/ftp/tevent/${pkgbase}-${pkgver}.tar.gz")
license=('GPL3')
depends=('talloc')
makedepends=('python2')
optdepends=('python2: for python bindings')

build() {
	cd ${srcdir}/${pkgbase}-${pkgver}

	# change to use python2
	sed -i -e "s|/usr/bin/env python$|/usr/bin/env python2|" buildtools/bin/waf
	export PYTHON=/usr/bin/python2

	./configure --prefix=/usr
}

package() {
	cd ${srcdir}/${pkgbase}-${pkgver}
	
	make
	make DESTDIR=${pkgdir}/ install
}
