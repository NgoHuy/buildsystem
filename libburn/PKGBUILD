# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Andreas Radke <andyrtr@archlinux.org>
# Original Contributor: William Rea <sillywilly@gmail.com>

pkgdesc="Library for reading, mastering and writing optical discs"
arch=('i686' 'x86_64')
url="http://libburnia-project.org"
license=('GPL')
depends=('glibc')
source=(http://files.libburnia-project.org/releases/${pkgbase}-${pkgver}.tar.gz{,.sig})
options=('!libtool' '!emptydirs')

build() {
  cd ${pkgbase}-${pkgver} # /.pl01/}
  ./configure --prefix=/usr --disable-static
  make
}

package() {
  cd ${pkgbase}-${pkgver} # /.pl01/}
  make DESTDIR=${pkgdir} install
}

