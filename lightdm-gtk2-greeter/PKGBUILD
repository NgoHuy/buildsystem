# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Maxime Gauduin <alucryd@gmail.com>
# Original Contributor: Padfoot <padfoot@exemail.com.au>

epoch=1
pkgdesc="GTK+ greeter for LightDM"
arch=('i686' 'x86_64')
url="https://launchpad.net/lightdm-gtk-greeter"
license=('GPL3' 'LGPL3')
depends=('gtk2' 'lightdm')
makedepends=('exo' 'gnome-doc-utils' 'gobject-introspection' 'intltool')
provides=("${pkgbase/2/}")
conflicts=("${pkgbase/2/3}")
backup=('etc/lightdm/lightdm-gtk-greeter.conf')
source=("https://launchpad.net/${pkgbase/2/}/1.6/${pkgver}/+download/${pkgbase/2/}-${pkgver}.tar.gz")

build() {
  cd "${srcdir}"/${pkgbase/2/}-${pkgver}

  ./configure --prefix=/usr --sbindir=/usr/bin --sysconfdir=/etc --libexecdir=/usr/lib/lightdm --disable-static --with-gtk2
  make
}

package() {
  cd "${srcdir}"/${pkgbase/2/}-${pkgver}

  make DESTDIR="${pkgdir}" install
}

# vim: ts=2 sw=2 et:
