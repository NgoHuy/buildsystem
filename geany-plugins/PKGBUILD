# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Alexander Rødseth <rodseth@gmail.com>
# Original Contributor: Angel 'angvp' Velasquez <angvp[at]archlinux.com.ve>  
# Original Contributor: djgera
# Original Contributor: Patrick Melo <patrick@patrickmelo.eti.br>

pkgdesc='Plugins for Geany'
arch=('x86_64' 'i686')
url='http://plugins.geany.org/'
license=('GPL')
depends=("geany=$pkgver" 'gtkspell' 'libwebkit' 'ctpl' 'gpgme' 'vte'
         'desktop-file-utils' 'gtk-update-icon-cache' 'lua51')
makedepends=('intltool' 'vala' 'gdb' 'cppcheck')
optdepends=('hspell: hebrew spell checker')
options=('!libtool')
install="$pkgbase.install"
source=("http://plugins.geany.org/$pkgbase/$pkgbase-$pkgver.tar.bz2")

build() {
  cd "$srcdir/$pkgbase-$pkgver"

  # The geanylua plugin currently doesn't work with lua 5.2, only 5.1
  ./configure --prefix=/usr --libexecdir=/usr/lib
  make 
}

package() {
  cd "$srcdir/$pkgbase-$pkgver"

  make DESTDIR="$pkgdir" install
}

# vim:set ts=2 sw=2 et:
