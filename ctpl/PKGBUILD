# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Alexander Rødseth <rodseth@gmail.com>
# Original Contributor: Angel Velasquez <angvp@archlinux.org>   
# Original Contributor: Patrick Melo <patrick@patrickmelo.eti.br>
pkgdesc="Template engine library written in C"
arch=('x86_64' 'i686')
url="http://ctpl.tuxfamily.org/"
license=('GPL')
depends=('glib2')
options=(!libtool)
source=("http://download.tuxfamily.org/ctpl/releases/$pkgbase-$pkgver.tar.gz")

build() {
  cd "$srcdir/$pkgbase-$pkgver"

  ./configure --prefix=/usr
  make 
}

package() {
  cd "$srcdir/$pkgbase-$pkgver"

  make DESTDIR="$pkgdir" install
}

# vim:set ts=2 sw=2 et:
