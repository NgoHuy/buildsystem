# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer:
# Original Contributor: Andrea Scarpino <andrea@archlinux.org>
# Original Contributor: Paul Mattal <paul@archlinux.org>
# Original Contributor: Tom Newsom <Jeepster@gmx.co.uk>

pkgdesc="A set of free application programs implementing FTP"
url="http://www.ncftp.com/"
arch=('i686' 'x86_64')
license=('custom')
depends=('ncurses')
source=("ftp://ftp.ncftp.com/ncftp/$pkgbase-$pkgver-src.tar.bz2")

build() {
  cd "${srcdir}/${pkgbase}-${pkgver}"

  ./configure --prefix=/usr \
    --sysconfdir=/etc
  make
}

package() {
  cd "${srcdir}/${pkgbase}-${pkgver}"
  install -d "${pkgdir}/usr/share"
  make prefix="${pkgdir}/usr" mandir="${pkgdir}/usr/share/man" install

  # Install license
  install -Dm644 doc/LICENSE.txt \
    "${pkgdir}/usr/share/licenses/${pkgbase}/LICENSE"
}
