# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Stéphane Gaudreault <stephane@archlinux.org>
# Original Contributor: Allan McRae <allan@archlinux.org>
# Original Contributor: Andreas Radke <andyrtr@archlinux.org>

pkgdesc="A tool to display dialog boxes from shell scripts"
arch=('i686' 'x86_64')
url="http://invisible-island.net/dialog/"
license=('LGPL2.1')
depends=('ncurses')
source=(ftp://invisible-island.net/${pkgbase}/${pkgbase}-${pkgver/_/-}.tgz)

build() {
  cd "${srcdir}/$pkgbase-${pkgver/_/-}"
  ./configure --prefix=/usr --mandir=/usr/share/man \
              --with-ncursesw --enable-nls
  make
}

package() {
  cd "${srcdir}/$pkgbase-${pkgver/_/-}"
  make DESTDIR="${pkgdir}" install install-lib
}
