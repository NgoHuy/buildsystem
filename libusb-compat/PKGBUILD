# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer:
# Original Contributor: Tobias Powalowski <tpowa@archlinux.org>
# Original Contributor: arjan <arjan@archlinux.org>

pkgdesc="Library to enable user space application programs to communicate with USB devices"
arch=('i686' 'x86_64')
url="http://libusb.sourceforge.net/"
license=('LGPL')
depends=('libusb' 'sh')
options=('!libtool')
source=("http://downloads.sourceforge.net/${pkgbase%-*}/${pkgbase}-${pkgver%.*}/$pkgbase-$pkgver/${pkgbase}-${pkgver}.tar.bz2")

build() {
  cd "${srcdir}/${pkgbase}-${pkgver}"
  ./configure --prefix=/usr --disable-static
  make
}

package() {
  cd "${srcdir}/${pkgbase}-${pkgver}"
  make DESTDIR="${pkgdir}" install
}

