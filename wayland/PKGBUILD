# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Tom Gundersen <teg@jklm.no>
# Original Contributor: Sébastien Luttringer
# Original Contributor: Joel Teichroeb <joel@teichroeb.net>

pkgdesc='A computer display server protocol'
arch=('i686' 'x86_64')
url='http://wayland.freedesktop.org'
license=('MIT')
depends=('libffi' 'expat')
makedepends=('doxygen')
options=(!libtool)
source=("http://wayland.freedesktop.org/releases/$pkgbase-$pkgver.tar.xz")

build() {
  cd $pkgbase-$pkgver

  ./configure --prefix=/usr \
    --disable-static
  make
}

package() {
  cd $pkgbase-$pkgver

  make DESTDIR="$pkgdir" install
  install -Dm 644 COPYING "$pkgdir/usr/share/licenses/$pkgbase/COPYING"
}
