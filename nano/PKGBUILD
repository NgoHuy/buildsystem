#!/bin/bash
# Maintainer: Anh K. Huynh <kyanh@theslinux.org>
# Original Maintainer: Andreas Radke <andyrtr@archlinux.org>
# Original Contributor: Judd <judd@archlinux.org>

pkgdesc="Pico editor clone with enhancements"
arch=('i686' 'x86_64')
license=('GPL')
url="http://www.nano-editor.org"
groups=('base')
depends=('ncurses' 'sh')
backup=('etc/nanorc')
install=nano.install
source=(http://www.nano-editor.org/dist/v2.2/${pkgbase}-${pkgver}.tar.gz)

build() {
  cd ${srcdir}/${pkgbase}-${pkgver}
  ./configure --prefix=/usr \
    --sysconfdir=/etc \
    --enable-color \
    --enable-nanorc \
    --enable-multibuffer \
    --disable-wrapping-as-root
  make
}

package() {
  cd ${srcdir}/${pkgbase}-${pkgver}
  make DESTDIR=${pkgdir} install
  install -DTm644 ${srcdir}/${pkgbase}-${pkgver}/doc/nanorc.sample ${pkgdir}/etc/nanorc
}
