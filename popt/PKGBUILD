# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Stéphane Gaudreault <stephane@archlinux.org>
# Original Contributor: Andrea Scarpino <andrea@archlinux.org>
# Original Contributor: dorphell <dorphell@archlinux.org>
# Original Contributor: Judd Vinet <jvinet@zeroflux.org>
# Original Contributor: John Proctor <jproctor@prium.net>

pkgdesc="A commandline option parser"
arch=('i686' 'x86_64')
url="http://rpm5.org"
license=('custom')
depends=('glibc')
options=('!libtool')
source=(http://rpm5.org/files/${pkgbase}/${pkgbase}-${pkgver}.tar.gz)

build() {
  cd "${srcdir}/${pkgbase}-${pkgver}"

  ./configure --prefix=/usr
  make
}

package() {
  cd "${srcdir}/${pkgbase}-${pkgver}"
  make DESTDIR="${pkgdir}" install

  install -Dm644 COPYING "${pkgdir}"/usr/share/licenses/${pkgbase}/LICENSE
}
