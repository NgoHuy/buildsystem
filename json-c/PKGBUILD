# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Jan Alexander Steffens (heftig) <jan.steffens@gmail.com>
# Original Contributor: Geoffroy Carrier <geoffroy.carrier@koon.fr>
# Original Contributor: congyiwu <congyiwu AT gmail DOT com>
pkgdesc="A JSON implementation in C"
url="https://github.com/json-c/json-c/wiki"
license=(MIT)
arch=(i686 x86_64)
depends=(glibc)
options=(!libtool !makeflags)
source=(https://s3.amazonaws.com/${pkgbase}_releases/releases/$pkgbase-${pkgver}.tar.gz)

build() {
  cd $pkgbase-$pkgver
  ./configure --prefix=/usr --disable-static
  make
}

package() {
  cd $pkgbase-$pkgver
  make DESTDIR="$pkgdir" install
  install -Dm644 COPYING "$pkgdir/usr/share/licenses/$pkgbase/COPYING"
}
