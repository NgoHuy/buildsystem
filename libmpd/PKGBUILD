# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Andrea Scarpino <andrea@archlinux.org>
# Original Contributor: tobias <tobias@archlinux.org>

pkgdesc="Signal based wrapper around libmpdclient"
arch=('i686' 'x86_64')
url="http://gmpc.wikia.com/wiki/Gnome_Music_Player_Client"
license=('GPL')
depends=('glib2')
options=('!libtool')
source=(http://download.sarine.nl/Programs/gmpc/${pkgver%.*}/${pkgbase}-${pkgver}.tar.gz)

build() {
  cd "${srcdir}/$pkgbase-$pkgver"
  ./configure --prefix=/usr
  make
}

package() {
  cd "${srcdir}/$pkgbase-$pkgver"
  make DESTDIR="${pkgdir}" install
}
