# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Giovanni Scafora <giovanni@archlinux.org>
# Original Contributor: Tom Newsom <Jeepster@gmx.co.uk>

pkgdesc="The whois client by Marco d'Itri"
arch=('i686' 'x86_64')
url="http://www.linux.it/~md/software/"
license=('GPL')
depends=('libidn')
makedepends=('perl')
source=("http://ftp.debian.org/debian/pool/main/w/whois/${pkgbase}_${pkgver}.tar.xz")

build() {
  cd "${srcdir}/${pkgbase}-${pkgver}"

  make prefix=/usr CFLAGS="$CFLAGS" HAVE_LIBIDN=1 HAVE_ICONV=1
}

package() {
  cd "${srcdir}/${pkgbase}-${pkgver}"

  make prefix=/usr BASEDIR="${pkgdir}" install-whois
}
