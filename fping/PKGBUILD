# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Giovanni Scafora <giovanni@archlinux.org>
# Original Contributor: Tom Newsom <Jeepster@gmx.co.uk>

pkgdesc="A utility to ping multiple hosts at once"
arch=('i686' 'x86_64')
url="http://www.fping.org/"
license=('custom') 
depends=('glibc')
source=("http://www.fping.org/dist/${pkgbase}-${pkgver}.tar.gz")

build() {
  cd "$srcdir/$pkgbase-$pkgver"

  ./configure --prefix=/usr \
              --sbindir=/usr/bin \
              --mandir=/usr/share/man \
              --enable-ipv4 \
              --enable-ipv6
  make
}

package() {
  cd "$srcdir/$pkgbase-$pkgver"

  make DESTDIR="${pkgdir}" install

  # install license
  install -D -m644 COPYING "${pkgdir}/usr/share/licenses/$pkgbase/LICENSE"
  chmod 4555 ${pkgdir}/usr/bin/fping
}
