# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Andrea Scarpino <andrea@archlinux.org>
# Original Contributor: Tobias Powalowski <tpowa@archlinux.org>

pkgdesc="A C library for reading, creating, and modifying zip archives"
url="http://www.nih.at/libzip/index.html"
license=('BSD')
arch=('i686' 'x86_64')
depends=('zlib')
options=('!libtool')
source=("http://www.nih.at/${pkgbase}/${pkgbase}-${pkgver}.tar.xz"
        'fix-headers.patch')

prepare() {
  cd ${pkgbase}-${pkgver}
  patch -p1 -i "${srcdir}/fix-headers.patch"
}

build() {
  cd ${pkgbase}-${pkgver}
  autoreconf -i
  ./configure --prefix=/usr
  make
}

package() {
  cd ${pkgbase}-${pkgver}
  make DESTDIR="${pkgdir}" install
  install -Dm644 LICENSE "${pkgdir}/usr/share/licenses/${pkgbase}/LICENSE"
}
