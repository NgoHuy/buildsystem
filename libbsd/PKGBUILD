# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Sven-Hendrik Haase <sh@lutzhaase.com>
# Original Contributor: Gerardo Exequiel Pozzi <vmlinuz386@yahoo.com.ar>

pkgdesc="Provides useful functions commonly found on BSD systems like strlcpy()"
arch=('i686' 'x86_64')
url="http://libbsd.freedesktop.org"
license=('custom')
depends=('glibc')
options=('!libtool')
source=(http://libbsd.freedesktop.org/releases/$pkgbase-$pkgver.tar.xz{,.asc})

build() {
  cd $pkgbase-$pkgver

  ./configure --prefix=/usr
  make
}

check() {
  cd $pkgbase-$pkgver

  make check
}

package() {
  cd $pkgbase-$pkgver

  make DESTDIR="$pkgdir" install
  install -D -m644 COPYING "$pkgdir/usr/share/licenses/$pkgbase/LICENSE"
}

# vim:set ts=2 sw=2 et:
