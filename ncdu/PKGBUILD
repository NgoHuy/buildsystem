# Maintainer: Severus <severus@theslinux.org>

# Original Contributor: lp76 <l.peduto@gmail.com>
# Original Contributor: Daenyth <Daenyth+Arch AT gmail DOT com>
# Original Maintainer: Gaetan Bisson <bisson@archlinux.org>

pkgdesc='Disk usage analyzer with an ncurses interface'
url='http://dev.yorhel.nl/ncdu/'
license=('custom:MIT')
depends=('ncurses')
arch=('i686' 'x86_64')
source=("http://dev.yorhel.nl/download/${pkgbase}-${pkgver}.tar.gz")

build() {
	cd "${srcdir}/${pkgbase}-${pkgver}"
	./configure --prefix=/usr
	make
}

package() {
	cd "${srcdir}/${pkgbase}-${pkgver}"
	make DESTDIR="${pkgdir}" install
	install -Dm644 COPYING "${pkgdir}/usr/share/licenses/${pkgbase}/LICENSE"
}
