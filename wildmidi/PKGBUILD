# Original Maintainer: Jan de Groot <jgc@archlinux.org>
# Original Contributor: Jaroslav Lichtblau <dragonlord@aur.archlinux.org>
# Original Contributor: Panagiotis Papadopoulos

pkgdesc='Open Source MIDI Synthesizer'
arch=('i686' 'x86_64')
url='http://wildmidi.sourceforge.net/'
license=('LGPL3')
depends=('alsa-lib')
options=(!makeflags !libtool)
source=(http://sourceforge.net/projects/wildmidi/files/${pkgbase}/${pkgbase}-${pkgver}.tar.gz)

build() {
  cd "${srcdir}/${pkgbase}-${pkgver}"

  ./configure --prefix=/usr --disable-werror
  make
}

package() {
  cd "${srcdir}/${pkgbase}-${pkgver}"
  make DESTDIR="${pkgdir}" install
}
