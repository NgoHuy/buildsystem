# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Gaetan Bisson <bisson@archlinux.org>
# Original Contributor: Ashish Shukla <wahjava.ml@gmail.com>

pkgdesc='OpenType Font library'
url='http://www.nongnu.org/m17n/'
license=('LGPL')
options=('!libtool')
arch=('i686' 'x86_64')
depends=('libxaw' 'freetype2')
source=("http://download.savannah.gnu.org/releases/m17n//${pkgbase}-${pkgver}.tar.gz")

build() {
	cd "${srcdir}/${pkgbase}-${pkgver}"
	./configure --prefix=/usr --disable-static
	make
}

package() {
	cd "${srcdir}/${pkgbase}-${pkgver}"
	make DESTDIR="${pkgdir}" install
}
