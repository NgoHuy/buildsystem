# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Andrea Scarpino <andrea@archlinux.org>
# Original Contributor: Pierre Schmitz <pierre@archlinux.de>
# Original Contributor: Sergej Pupykin <pupykin.s+arch@gmail.com>

pkgdesc='Independent Open DataBase Connectivity for Linux'
arch=('i686' 'x86_64')
url="http://www.iodbc.org/dataspace/iodbc/wiki/iODBC/"
license=('LGPL')
depends=('sh')
makedepends=('chrpath')
options=('!libtool')
source=("http://downloads.sourceforge.net/iodbc/${pkgbase}-${pkgver}.tar.gz")

build() {
	cd ${pkgbase}-${pkgver}
	./configure --prefix=/usr \
		--disable-static \
		--includedir=/usr/include/libiodbc \
		--disable-gui \
        --disable-libodbc
	make
}

package() {
	cd ${pkgbase}-${pkgver}
	make DESTDIR=${pkgdir} install

	chrpath -d ${pkgdir}/usr/bin/iodbctest{,w}
	rm -rf ${pkgdir}/usr/share/libiodbc/
}
