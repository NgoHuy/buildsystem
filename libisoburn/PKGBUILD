# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Andreas Radke <andyrtr@archlinux.org>
# Original Contributor: Gour <Gour <gour@mail.inet.hr>

pkgdesc="frontend for libraries libburn and libisofs"
url="http://libburnia-project.org"
arch=('i686' 'x86_64')
license=('GPL2')
depends=('libburn>=1.3.2' 'libisofs>=1.3.2' 'readline')
options=('!libtool')
install=${pkgbase}.install
source=(http://files.libburnia-project.org/releases/${pkgbase}-${pkgver}.tar.gz{,.sig})

build() {
  cd ${pkgbase}-${pkgver}
  ./configure --prefix=/usr
  make
}

package() {
  cd ${pkgbase}-${pkgver}
  make DESTDIR=${pkgdir} install
}
