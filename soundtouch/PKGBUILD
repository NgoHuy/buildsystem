# Maintainer: Severus <severus@theslinux.org>
# Original Maintainer: Eric Bélanger <eric@archlinux.org>

pkgdesc="An audio processing library"
arch=('i686' 'x86_64')
url="http://www.surina.net/soundtouch/"
license=('LGPL')
depends=('gcc-libs')
options=('!libtool')
source=(http://www.surina.net/soundtouch/${pkgbase}-${pkgver}.tar.gz)

build() {
  cd "${srcdir}/${pkgbase}"
  [ "${CARCH}" = "x86_64" ] && CONFIGFLAG="--with-pic"
  sed -i 's/AM_CONFIG_HEADER/AC_CONFIG_HEADER/' configure.ac
  ./bootstrap
  ./configure --prefix=/usr --enable-shared ${CONFIGFLAG}
  make
}

package() {
  cd "${srcdir}/${pkgbase}"
  make DESTDIR="${pkgdir}" pkgdocdir=/usr/share/doc/soundtouch install
}
